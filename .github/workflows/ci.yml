name: CI/CD

on:
  push:
    branches: [main, develop, feat/**]
  pull_request:
    branches: [main, develop]

# Push: Runs when code is pushed directly to branches
# Pull Request: Runs when a PR is created/updated to check if code is safe to merge

jobs:
  # Smart Contract Tests
  contracts:
    name: Test Smart Contracts
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install Clarinet
        run: |
          wget -nv https://github.com/hirosystems/clarinet/releases/latest/download/clarinet-linux-x64-glibc.tar.gz
          tar -xf clarinet-linux-x64-glibc.tar.gz
          sudo mv clarinet /usr/local/bin
          clarinet --version

      - name: Install dependencies
        run: npm install

      - name: Run contract tests
        run: npm run test

  # Frontend Build
  frontend-build:
    name: Build Frontend
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install frontend dependencies
        run: cd frontend && npm install

      - name: Build frontend
        run: cd frontend && npm run build

      - name: Verify build artifacts
        run: test -d frontend/dist && echo "✓ Build successful"

  # CI Summary
  ci-summary:
    name: CI/CD Summary
    runs-on: ubuntu-latest
    needs: [contracts, frontend-build]
    if: always()

    steps:
      - name: Check job statuses
        run: |
          if [ "${{ needs.contracts.result }}" = "success" ] && [ "${{ needs.frontend-build.result }}" = "success" ]; then
            echo "✓ All CI checks passed"
            exit 0
          else
            echo "✗ Some CI checks failed"
            exit 1
          fi