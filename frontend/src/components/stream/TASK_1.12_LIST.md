# StreamList Component
Tab-based stream list with:
- Sending tab: streams user created
- Receiving tab: streams user is recipient of
- Empty state message when no streams
- Loading skeleton placeholders
- Sort and filter options
